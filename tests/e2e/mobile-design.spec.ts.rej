@@ -66,9 +66,11 @@
   test('Chat Area & Input Area', async ({ page }) => {
     // Start a new chat
     await page.waitForSelector('button:has-text("New Conversation")');
-    await page.click('button:has-text("New Conversation")');
-    await page.waitForTimeout(500);
-    await page.click('button:has-text("New Conversation")');
+    const btn = page.locator('button:has-text("New Conversation")');
+    await btn.click();
+    if (await btn.isVisible()) {
+      await btn.click(); // retry if still visible (hydration issue)
+    }
     await page.waitForURL('**/chat/**');
 
     // Send a message
